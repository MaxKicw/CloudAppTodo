{
  "name": "backend",
  "version": "0.0.1",
  "description": "nightproject",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node server.js"
  },
  "author": "Max",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "mongodb": "^3.1.7",
    "mongoose": "^5.3.2",
    "pusher": "^2.1.3"
  }
}
